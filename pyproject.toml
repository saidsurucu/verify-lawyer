[project]
name = "verify-lawyer"
version = "1.0.0"
description = "Turkish Lawyer Verification CLI and Python Module"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.1.0",
    "playwright>=1.40.0",
]
